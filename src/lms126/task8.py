from to_do import TODO


def task8():

    # For each multiple of 3, print “Foo” instead of the number.
    # For each multiple of 5, print “Bar” instead of the number.
    # For numbers which are multiples of both 3 and 5, print “FooBar” instead of the number.
    # Else print "Qix"

    return """
    Input number X
    If x is multiples of 3 
    Output = print "Foo"
    Else
    If x is multiples of 5 then 
    Output = print "Bar"
    Else
    If x is multiples of 3 and 5 
    Output= print "FooBar"
    Else 
    output= print "Qix"
    End
    
"""
